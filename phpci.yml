setup:
    shell:
        - "pwd > /var/www/pwd.txt && mkdir -p _althingi/module/Althingi"

test:
    php_code_sniffer:
        path: "src"
        standard: "PSR2"

    php_unit:
        config:
            - "tests/phpunit.xml"
        path: "tests/"
