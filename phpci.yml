setup:
    shell:
        - "mkdir -p /var/www/phpci/PHPCI/build/_althingi/module/Althingi"

    copy_build:
        directory: "/var/www/phpci/PHPCI/build/_althingi/module/Althingi"

test:
    php_code_sniffer:
        path: "src"
        standard: "PSR2"

    php_unit:
        config:
            - "tests/phpunit.xml"
        path: "tests/"
