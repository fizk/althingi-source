setup:
    shell:
        - "echo %buildpath% > /var/www/place.txt && mkdir -p %buildpath%/../_althingi/module/Althingi"

test:
    php_code_sniffer:
        path: "src"
        standard: "PSR2"

    php_unit:
        config:
            - "tests/phpunit.xml"
        path: "tests/"
